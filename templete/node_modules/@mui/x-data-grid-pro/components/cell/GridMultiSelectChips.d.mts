import * as React from 'react';
import type { GridSlotProps, ValueOptions } from '@mui/x-data-grid';
import type { DataGridProProcessedProps } from "../../models/dataGridProProps.mjs";
type OwnerState = DataGridProProcessedProps;
export declare const MultiSelectChipsRoot: import("@emotion/styled").StyledComponent<import("@mui/system").MUIStyledCommonProps<import("@mui/material/styles").Theme> & {
  ownerState: OwnerState;
}, React.DetailedHTMLProps<React.HTMLAttributes<HTMLDivElement>, HTMLDivElement>, {}>;
export interface GridMultiSelectChipsProps<V extends ValueOptions = ValueOptions> extends Omit<React.HTMLAttributes<HTMLDivElement>, 'children'> {
  values: any[];
  field: string;
  columnWidth: number;
  optionByValue: Map<any, ValueOptions>;
  getOptionLabel: (option: ValueOptions) => string;
  /** When true, skip overflow measurement and render every chip (for auto-row-height rows). */
  autoWrap?: boolean;
  classes?: {
    root?: string;
    chip?: string;
    chipHidden?: string;
    overflow?: string;
  };
  slotProps?: {
    root?: React.HTMLAttributes<HTMLDivElement>;
    chip?: Partial<GridSlotProps['baseChip']> | ((value: V, index: number) => Partial<GridSlotProps['baseChip']>);
    /** Props for the `+N` overflow chip. Omit to render it as a non-interactive indicator. */
    overflow?: Partial<GridSlotProps['baseChip']>;
  };
}
export declare const GridMultiSelectChips: <V extends ValueOptions = ValueOptions>(props: GridMultiSelectChipsProps<V> & {
  ref?: React.ForwardedRef<HTMLDivElement>;
}) => React.ReactElement;
export {};